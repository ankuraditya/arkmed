VITE_API_BASE_URL=http://localhost:8000/api/v1
# Keep false until verified medicine data and secure prescription uploads are connected.
VITE_ENABLE_API_ORDERS=false
VITE_ENABLE_LIVE_CATALOGUE=false
# Production must use an HTTPS API URL and both feature flags should only be enabled after catalogue and prescription-storage verification.
