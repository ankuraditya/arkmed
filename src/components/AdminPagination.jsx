export default function AdminPagination({meta,onPage}){if(!meta||meta.last_page<=1)return null;const go=page=>{onPage(page);document.querySelector('.admin-heading')?.scrollIntoView({behavior:'smooth'})};return <nav className="admin-pagination" aria-label="Pagination"><button disabled={meta.current_page<=1} onClick={()=>go(1)} aria-label="First page">First</button><button disabled={meta.current_page<=1} onClick={()=>go(meta.current_page-1)}>Previous</button><span aria-live="polite">Page {meta.current_page} of {meta.last_page} · {meta.total} records</span><button disabled={meta.current_page>=meta.last_page} onClick={()=>go(meta.current_page+1)}>Next</button><button disabled={meta.current_page>=meta.last_page} onClick={()=>go(meta.last_page)} aria-label="Last page">Last</button></nav>}
