import { useEffect } from 'react'
const fallbackDescription='Order medicines on WhatsApp, upload prescriptions and request healthcare services from ARK med Pharmacy & Clinic.'
function setMeta(property,value,attribute='name'){let element=document.head.querySelector(`meta[${attribute}="${property}"]`);if(!element){element=document.createElement('meta');element.setAttribute(attribute,property);document.head.appendChild(element)}element.setAttribute('content',value)}
export default function Seo({title,description=fallbackDescription,noIndex=false,image='/og-arkmed.jpg',imageAlt='ARK med Pharmacy & Clinic — Your Health. Our Priority.',type='website'}){useEffect(()=>{const fullTitle=title?`${title} | ARK med`:'ARK med Pharmacy & Clinic | Your Health. Our Priority.';const canonicalUrl=`${window.location.origin}${window.location.pathname}`;const imageUrl=new URL(image,window.location.origin).href;document.title=fullTitle;const robots=noIndex?'noindex, nofollow':'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1';setMeta('description',description);setMeta('robots',robots);setMeta('googlebot',noIndex?'noindex, nofollow':'index, follow, max-image-preview:large');setMeta('og:site_name','ARK med Pharmacy & Clinic','property');setMeta('og:locale','en_IN','property');setMeta('og:title',fullTitle,'property');setMeta('og:description',description,'property');setMeta('og:type',type,'property');setMeta('og:url',canonicalUrl,'property');setMeta('og:image',imageUrl,'property');setMeta('og:image:secure_url',imageUrl,'property');setMeta('og:image:type','image/jpeg','property');setMeta('og:image:width','1200','property');setMeta('og:image:height','630','property');setMeta('og:image:alt',imageAlt,'property');setMeta('twitter:card','summary_large_image');setMeta('twitter:title',fullTitle);setMeta('twitter:description',description);setMeta('twitter:image',imageUrl);setMeta('twitter:image:alt',imageAlt);let canonical=document.head.querySelector('link[rel="canonical"]');if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)}canonical.href=canonicalUrl},[title,description,noIndex,image,imageAlt,type]);return null}
