import { Component } from 'react'
export default class ErrorBoundary extends Component{constructor(props){super(props);this.state={error:null}}static getDerivedStateFromError(error){return{error}}componentDidCatch(error,info){console.error('ARK med UI error',error,info)}render(){if(this.state.error)return <main className="fatal-error"><img src="/logo.png" alt="ARK med"/><h1>Something went wrong</h1><p>The page could not be displayed. Your cart and saved items remain on this device.</p><button className="button primary" onClick={()=>location.reload()}>Reload application</button><a href="/">Return home</a></main>;return this.props.children}}
